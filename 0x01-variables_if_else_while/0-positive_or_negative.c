#include <stdlib.h>
#include <stdio.h>
/**
 *main - Entry point
 *
 *Return: always 0
 */
void main ()
{
int n;
 printf("input a number")
 scanf("%d", &num);
 if (num > 0)
 printf("%d is a positive \n", num);
 else if (num < 0)
 printf("%d is a negative \n", num);
 else
 printf("%d is zero \n", num);
}
 
